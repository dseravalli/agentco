{
  "name": "agentco",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "cli": "tsx packages/cli/src/index.ts"
  },
  "engines": {
    "node": ">=20"
  }
}
